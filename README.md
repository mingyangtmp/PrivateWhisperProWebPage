PrivateWhisper GitHub Pages site.

This repository publishes a static website for the PrivateWhisper product, including:

- landing page
- privacy policy
- support page

The site is deployed with GitHub Pages via `.github/workflows/deploy-pages.yml`.
